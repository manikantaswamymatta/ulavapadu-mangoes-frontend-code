[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m pip install --upgrade pip", "pip install -r requirements.railway.txt"]

[start]
cmd = "uvicorn unified_backend.main:app --host 0.0.0.0 --port $PORT"
