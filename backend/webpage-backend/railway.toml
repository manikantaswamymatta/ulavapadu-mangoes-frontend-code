[build]
builder = "NIXPACKS"
buildCommand = "echo 'Using Nixpacks install phase'"

[deploy]
startCommand = "/opt/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
